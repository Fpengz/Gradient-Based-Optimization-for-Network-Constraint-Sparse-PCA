\documentclass[10pt,twocolumn,letterpaper]{article}

% =========================
% Packages
% =========================
\usepackage[margin=0.75in]{geometry}
\usepackage{amsmath, amssymb, amsthm}
\usepackage{mathtools}
\usepackage{microtype}
\usepackage{graphicx}
\graphicspath{{figures/}{../figures/}{../figures/realdata/}}
\usepackage{subcaption}
\usepackage{booktabs}
\usepackage{multirow}
\usepackage{array}
\usepackage{tabularx}
\usepackage[hypertexnames=false]{hyperref}
\usepackage{url}
\usepackage{algorithm}
\usepackage{algpseudocode}
\usepackage{enumitem}

% =========================
% Theorem Environments
% =========================
\newtheorem{theorem}{Theorem}
\newtheorem{lemma}{Lemma}
\newtheorem{proposition}{Proposition}
\newtheorem{corollary}{Corollary}
\theoremstyle{definition}
\newtheorem{definition}{Definition}
\newtheorem{assumption}{Assumption}
\newtheorem{remark}{Remark}

% =========================
% Commands
% =========================
\newcommand{\R}{\mathbb{R}}
\newcommand{\stiefel}{\mathrm{St}}
\newcommand{\Sigmahat}{\widehat{\Sigma}}
\DeclareMathOperator{\prox}{prox}
\DeclareMathOperator{\soft}{soft}
\DeclareMathOperator{\supp}{supp}
\DeclareMathOperator{\tr}{tr}
\DeclareMathOperator{\sym}{sym}
\DeclareMathOperator{\dist}{dist}
\newcolumntype{Y}{>{\raggedright\arraybackslash}X}
\setlength{\columnsep}{0.25in}
\setlength{\emergencystretch}{3em}
\hbadness=10000
\graphicspath{{figures/}{../figures/}}

% =========================
% Title
% =========================
\title{\texorpdfstring{Graph-Smooth Sparse Orthogonal PCA via Alternating Manifold and Graph-Coupled Proximal Updates}{Graph-Smooth Sparse Orthogonal PCA via Alternating Manifold and Graph-Coupled Proximal Updates}}
\author{Wang Zhoufu}
\date{}

\begin{document}
\sloppy
\raggedbottom
\maketitle

% =========================
% Abstract
% =========================
\begin{abstract}
We present a controlled empirical study of sparse, graph-smooth, explicitly orthogonal multi-component PCA through a split-variable formulation that couples an orthogonal loading matrix with a sparse graph-regularized proxy. Rather than proposing new optimization primitives, we use this minimal solver as a testbed to characterize when graph regularization helps or hurts under misspecification. Experiments spanning chain, grid, SBM, and kNN graphs show a regime-based pattern: weak regularization is robust across topologies, while stronger regularization can amplify misspecification on fragile graphs. Real-data studies on TCGA-BRCA, MNIST (grid), and S\&P500 correlation graphs show aligned trends. We further show that robustness to graph misspecification is strongly topology-dependent: sparse, fragile graphs degrade under corruption, while redundant graphs (e.g., grids and kNN) are substantially more stable and can mitigate oversmoothing. Our contribution is thus an empirical characterization of topology- and regime-dependent behavior rather than a universal performance gain.
\end{abstract}

\section{Introduction}
\label{sec:introduction}

Principal component analysis (PCA) is a foundational technique for dimensionality reduction, exploratory analysis, and low-dimensional representation learning. Its appeal stems from its simplicity and its optimality for variance maximization under orthogonality constraints. However, classical PCA typically produces dense loading vectors, so that each principal component depends on nearly all input variables. In modern high-dimensional applications such as genomics, neuroimaging, and structured sensing, these dense loadings can be difficult to interpret and of limited use for scientific discovery or feature selection \cite{zou2006spca,chen2020amanpg}.

Sparse principal component analysis (SPCA) addresses this limitation by encouraging principal components to depend on only a small subset of variables. Over the past two decades, SPCA has been studied through several complementary formulations, including regression-based relaxations, power-method variants, and nonsmooth constrained optimization approaches \cite{zou2006spca,journee2010gpower,chen2020amanpg}. More recent work has emphasized the importance of handling sparsity and orthogonality in a principled manner, rather than relying solely on heuristic thresholding or post hoc orthogonalization. In particular, manifold-based formulations such as alternating manifold proximal-gradient methods show that sparse multi-component estimation can be treated directly as a nonsmooth optimization problem with orthogonality constraints, yielding stronger algorithmic structure and convergence guarantees than purely heuristic alternatives \cite{chen2020amanpg,chen2024nonsmoothstiefel}.

Despite these advances, sparsity alone is often not enough. In many high-dimensional domains, features are not exchangeable but are linked through known relational structure. Genes participate in pathways and interaction networks, voxels exhibit spatial or functional connectivity, and pixels or spectral bands obey spatial adjacency and smoothness patterns. When such prior structure is available, it is natural to ask whether the extracted principal components should also respect it. A large literature on graph-aware and graph-regularized PCA studies how incorporating structural priors through graph-based penalties or low-frequency graph constraints can improve robustness and structural fidelity, especially when the signal is aligned with the underlying graph \cite{briola2026grpca}. At the same time, this literature also makes clear that graph-aware PCA is not a single unified model class: some methods emphasize low-frequency graph consistency, some include explicit sparsification, and others target application-specific clustering or representation objectives rather than sparse orthogonal component extraction \cite{briola2026grpca,feng2019glspca,wu2019gdspca,wang2025rsspca}.

A second challenge concerns the extraction of multiple components. In classical PCA, orthogonality follows directly from eigendecomposition. In sparse settings, however, orthogonality is no longer automatic, and sequential deflation can produce components that are only approximately orthogonal or that degrade cumulatively in quality. This has motivated a continued line of work on simultaneous sparse orthogonal extraction, including recent efforts that explicitly revisit orthogonal SPCA as an active optimization problem rather than a solved issue \cite{chen2020amanpg,bertsimas2023geospca,cheng2026orthogonalspca}. These developments suggest that, when multiple interpretable components are required, joint extraction under explicit orthogonality constraints remains preferable to treating sparsity as a purely post-processing step.

There is also a broader methodological backdrop. Beyond SPCA itself, recent work on nonsmooth and structured nonsmooth optimization over Riemannian submanifolds has substantially expanded the toolkit available for nonconvex problems with manifold constraints \cite{li2024structurednonsmooth,chen2024nonsmoothstiefel}. Moreover, adjacent problems such as sparse tensor canonical correlation analysis already combine Laplacian regularization, sparse penalties, manifold-based optimization, and modern inner solvers with convergence analysis \cite{zhu2025stccal}. Consequently, it is no longer accurate to position graph-coupled nonsmooth manifold optimization as an entirely unexplored conceptual territory. Rather, the more defensible and practically meaningful question is whether these ideas can be brought together in a coherent and effective framework for \emph{PCA proper}, with an explicit focus on sparse multi-component loading estimation under feature-graph smoothness and simultaneous orthogonality.

This paper studies that question. We consider a formulation that combines three desiderata: sparse loadings, smoothness with respect to a known feature graph, and joint orthogonality across multiple extracted components. Our goal is not to claim that graph-aware PCA, graph+sparsity PCA, or nonsmooth manifold optimization are individually new. Instead, we focus on a comparatively underexplored intersection of these themes: \emph{feature-graph Laplacian regularization, sparse multi-component loadings, and explicit simultaneous orthogonality within a modern manifold/proximal optimization framework for PCA}. This positioning is narrower than a broad novelty claim, but it better reflects the current state of the literature and the genuine methodological gap that remains \cite{briola2026grpca,feng2019glspca,wu2019gdspca,chen2020amanpg}.

Concretely, we introduce a graph-smooth sparse orthogonal PCA formulation in which orthogonality is handled through a Stiefel-constrained variable and structured sparse estimation is handled through a graph-regularized Euclidean variable coupled by a quadratic penalty. This split allows us to separate the geometry of orthogonality from the graph-coupled sparsity structure, while still optimizing the two jointly. The resulting viewpoint is particularly attractive in settings where interpretability is not just a matter of selecting a few variables, but of selecting variables whose pattern is coherent with known domain structure.

Our study is guided by two central questions. First, when does feature-graph regularization preserve recovery while improving structural coherence of sparse components? Second, what tradeoff does graph regularization induce between explained variance, graph consistency, and computational cost? These questions are important because graph-based regularization can help when the prior graph is informative, but may oversmooth the loadings or reduce variance capture when the graph is misspecified. Accordingly, the paper emphasizes not only model design and optimization, but also the empirical circumstances under which graph priors are beneficial.

We show that graph regularization in sparse PCA induces a topology-dependent regime behavior: it improves structural coherence consistently, but its effect on recovery depends on graph topology and regularization strength. This has practical implications in settings where the feature graph is uncertain or noisy (e.g., biological networks or financial correlations), as overly strong regularization can degrade recovery. Unlike prior graph-aware PCA work that focuses on performance improvements under aligned settings, our study explicitly characterizes failure modes under misspecification and shows that robustness is governed by graph topology. We intentionally use a minimal alternating proximal-gradient scheme to isolate the effect of graph regularization rather than optimization complexity.

\paragraph{Contributions.}
The main contributions of this work are as follows.
\begin{itemize}[leftmargin=*]
    \item Graph regularization consistently reduces the chosen graph-smoothness metric on the true graph, while its recovery impact depends on topology.
    \item On chain graphs, recovery remains competitive with sparse baselines while smoothness improves; on SBM graphs, stronger regularization induces a clearer recovery--smoothness tradeoff.
    \item In a graph-aligned chain setting with disjoint contiguous supports and scattered high-variance decoys, moderate regularization preserves recovery while improving support connectivity and smoothness.
    \item We empirically characterize how graph topology governs the effect of graph regularization under misspecification.
    \item We compare against graph-only PCA and a non-manifold graph+sparsity baseline to isolate the role of explicit orthogonality.
    \item We provide a concrete split-variable solver that combines explicit orthogonality, sparsity, and graph smoothness within a single executable framework.
\end{itemize}

Taken together, these contributions position the paper as a targeted integration of ideas that already exist in neighboring literatures, but whose precise combination for graph-smooth sparse orthogonal PCA remains comparatively underdeveloped. Our aim is therefore not to argue that every ingredient is new, but to show that their combination leads to a useful and well-motivated framework for structured dimensionality reduction in graph-informed high-dimensional settings.

\section{Related Work}
\label{sec:related}

\subsection{Sparse PCA and Orthogonal Component Extraction}
Sparse principal component analysis has been studied through regression-based relaxations, truncated power methods, penalized matrix decomposition, semidefinite relaxations, and nonsmooth constrained optimization \cite{zou2006spca,witten2009pmd,daspremont2007dspca,journee2010gpower,qiu2023gradientspca}. A central theme in this literature is that sparse loadings improve interpretability but complicate both optimization and the extraction of multiple components.

A particularly important line of work formulates SPCA directly as a nonsmooth optimization problem over orthogonality-constrained variables. Chen et al.\ proposed the alternating manifold proximal gradient framework (A-ManPG) for sparse PCA and sparse CCA, showing that sparse loadings and nonconvex orthogonality constraints can be handled within a unified manifold-based optimization scheme with convergence guarantees \cite{chen2020amanpg}. More broadly, the ManPG literature establishes a practical optimization template for smooth-plus-nonsmooth objectives on the Stiefel manifold \cite{chen2020manpg,chen2024nonsmoothstiefel}.

Orthogonal sparse extraction also remains an active topic rather than a closed one. GeoSPCA emphasizes simultaneous extraction over deflation for orthogonal sparse components \cite{bertsimas2023geospca}, and recent work on certifiably optimal orthogonal SPCA further underscores ongoing interest in jointly enforcing sparsity, orthogonality, and strong optimality properties \cite{cheng2026orthogonalspca}. These developments motivate explicit joint orthogonality rather than treating it as an afterthought.

\subsection{Graph-Aware PCA and Graph-Regularized Dimensionality Reduction}
A parallel literature incorporates structural priors into PCA through graphs or Laplacian-type regularization. These methods are motivated by the observation that in many high-dimensional applications, features are not exchangeable but related through biological, spatial, or network structure. Graph-aware PCA methods use this prior information to bias principal components toward graph-consistent directions, often by suppressing high-frequency variation and preserving graph-coherent low-frequency signals \cite{briola2026grpca,chung1997spectral,shuman2013gsp}.

Recent work makes it clear that graph-regularized PCA cannot simply be characterized as producing dense loadings. In particular, GR-PCA incorporates a learned sparse precision graph and regularizes PCA toward the low-frequency Fourier modes of the induced graph Laplacian; the method also evaluates sparsity-related behavior explicitly and reports improved structural fidelity, lower Laplacian energy, and competitive out-of-sample reconstruction under graph-aligned signals \cite{briola2026grpca}. This weakens any broad claim that graph-aware PCA is predominantly dense or uninterpretable.

At the same time, graph-aware PCA does not fully eliminate the gap relevant here. Many graph-regularized variants focus primarily on graph consistency, robustness, clustering structure, or low-frequency filtering, rather than on simultaneous sparse multi-component orthogonality in a single PCA-specific framework. Accordingly, the relevant distinction is no longer whether graph-aware PCA exists, but whether it addresses the specific combination of sparse loadings, feature-graph smoothness, and explicit joint orthogonality considered here.

\subsection{Graph + Sparsity PCA Variants}
Older and application-driven papers already show that graph structure and sparsity have been combined within PCA-like models. For example, gLSPCA integrates a graph Laplacian term and a sparsity constraint into PCA for gene-expression analysis \cite{feng2019glspca}, while GDSPCA combines graph Laplacian regularization with double sparsity constraints to improve interpretability and exploit internal geometry \cite{wu2019gdspca}. These papers are direct counterexamples to any claim that graph+sparsity PCA is absent from the literature.

However, these graph+sparsity PCA methods also illustrate why the gap must be phrased carefully. They are often application-specific, may target particular geometric or clustering objectives, and do not necessarily adopt the same optimization language as modern manifold proximal methods for joint orthogonal extraction. Recent applied work such as RSSPCA reinforces the same point: smooth sparse PCA variants continue to emerge in domain-specific settings, but they do not by themselves close the gap for feature-graph Laplacian regularization with explicit simultaneous orthogonal extraction in PCA proper \cite{wang2025rsspca}.

\subsection{Structured Sparse PCA}
Structured sparse PCA extends plain $\ell_1$-based sparsity by encouraging supports that follow meaningful patterns such as groups, trees, or contiguous regions. Jenatton, Obozinski, and Bach showed that structured regularization can impose higher-order information on the support of loading vectors and can improve interpretability relative to unstructured sparse PCA \cite{jenatton2010sspca}. This literature is highly relevant because it demonstrates that sparsity alone is often too weak a prior.

Nevertheless, structured sparse PCA in this sense is not identical to feature-graph smoothness. Structured sparsity typically constrains the support geometry through groups or shapes, whereas graph-Laplacian regularization encourages neighboring or strongly connected features to take similar values. These are related but distinct inductive biases, and the existence of structured sparse PCA does not by itself resolve the specific question of how to jointly impose graph smoothness, sparsity, and simultaneous orthogonality in multi-component PCA.

\subsection{Structured Nonsmooth Optimization on Manifolds}
The optimization backdrop for this problem has evolved substantially. Beyond the sparse-PCA-specific literature, recent work on nonsmooth and structured nonsmooth optimization over Riemannian submanifolds shows that manifold-based methods now handle a broader class of composite objectives than earlier sparse-only formulations \cite{li2024structurednonsmooth,chen2024nonsmoothstiefel}. Recent adjacent work also combines sparse regularization, Laplacian regularization, manifold proximal updates, semismooth Newton subproblem solvers, and convergence analysis in related problems such as sparse tensor CCA \cite{zhu2025stccal}. That literature does not solve our PCA formulation directly, but it does weaken any broad claim that the optimization ingredients themselves are unprecedented.

\subsection{Positioning of the Present Work}
Graph-aware PCA and graph-regularized sparse PCA have been studied in multiple forms, and we do not claim novelty in combining graph smoothness with sparsity. Unlike prior graph-aware PCA work that focuses on performance improvements under aligned settings, our study explicitly characterizes failure modes under misspecification and shows that robustness is governed by graph topology. The specific gap we target is the joint extraction of multiple explicitly orthogonal components under a split-variable formulation with a concrete, reproducible solver. Our work is positioned as a focused intersection of sparse PCA, graph-regularized PCA, and orthogonality-preserving optimization, rather than as a first instance of any single ingredient.

\section{Problem Setup}
\label{sec:setup}

Let $X \in \R^{n \times p}$ be a centered data matrix and let
\begin{equation}
    \Sigmahat = \frac{1}{n} X^\top X
\end{equation}
denote the sample covariance matrix. Let $G=(\mathcal{V},\mathcal{E},W)$ be a weighted undirected feature graph on the $p$ variables, with adjacency matrix $W$, degree matrix $D$, and graph Laplacian
\begin{equation}
    L = D - W.
\end{equation}
We assume $L \succeq 0$.
We use $\|M\|_1$ for the entrywise $\ell_1$ norm, $\soft(Z,\tau)$ for entrywise soft-thresholding at threshold $\tau$, and $\sym(M) = (M+M^\top)/2$ for the symmetric part of a matrix. Shared explained variance is computed after QR-based orthonormalization of the loading matrix.

Our goal is to extract $r$ principal components with loading structure that is simultaneously sparse, graph-smooth, and orthogonal. To separate the geometry of orthogonality from the structured regularization, we introduce two variables $A,B \in \R^{p \times r}$:
\begin{itemize}[leftmargin=*]
    \item $A$ carries the orthogonality constraint $A^\top A = I_r$,
    \item $B$ carries sparsity and graph smoothness.
\end{itemize}

We denote the Stiefel manifold by
\begin{equation}
    \stiefel(p,r) = \{ A \in \R^{p \times r} : A^\top A = I_r \}.
\end{equation}

\section{Proposed Formulation}
\label{sec:formulation}

We study the split formulation
\begin{equation}
\label{eq:main_obj}
\begin{aligned}
\min_{A \in \stiefel(p,r),\, B \in \R^{p \times r}} \quad
&-\tr(A^\top \Sigmahat A)
+ \lambda_1 \|B\|_1 \\
&+ \lambda_2 \tr(B^\top L B)
+ \frac{\rho}{2}\|A-B\|_F^2.
\end{aligned}
\end{equation}

The first term promotes variance capture; the $\ell_1$ term promotes sparse loadings; the Laplacian term promotes graph smoothness; and the quadratic coupling term forces the orthogonal variable $A$ and the structured loading variable $B$ to remain close.

\paragraph{Penalty coupling.}
The coupling term $\frac{\rho}{2}\|A-B\|_F^2$ is a fixed-$\rho$ quadratic penalty that enforces approximate consensus between $A$ and $B$; we do not use dual variables or ADMM-style updates.

\paragraph{Why split variables?}
Directly optimizing a sparse graph-regularized objective under orthogonality constraints is difficult because the orthogonality constraint is nonconvex while the graph-sparsity regularizer is nonsmooth and coupled across features. The split formulation isolates these effects: $A$ is updated on the Stiefel manifold, while $B$ is updated in Euclidean space under structured regularization.

We leave alternative sparsity structures to future work to keep the formulation fixed to entrywise $\ell_1$ on $B$.

\paragraph{Estimator choice.}
We treat $B$ as the primary estimator for sparsity and smoothness metrics, since it carries the graph-regularized structure. Variance is reported using $A$ (or $\mathrm{QR}(B)$ when needed) to ensure orthogonality.

\paragraph{Limiting cases.}
When $\lambda_2=0$, the formulation reduces to sparse PCA without graph smoothness; when $\lambda_1=0$, it reduces to graph-regularized PCA; and as $\rho\to\infty$, the split formulation converges to a single constrained problem with $A=B$.

\paragraph{Laplacian normalization.}
We use the normalized Laplacian $L=I-D^{-1/2}WD^{-1/2}$ throughout, so $\lambda_2$ is comparable across graphs with different degrees.

\section{Optimization Algorithm}
\label{sec:algorithm}

We solve the split-variable objective by alternating between two steps. The $A$-step updates an orthogonal loading matrix on the Stiefel manifold using a Riemannian gradient step followed by QR-based retraction. The $B$-step updates the sparse graph-smooth proxy with proximal gradient, treating the Laplacian quadratic and coupling term as the smooth part and the $\ell_1$ penalty as the nonsmooth part. This single solver path provides a minimal, reproducible baseline for the empirical study; more advanced variants remain future work.

\subsection{A-Step: Manifold Update}
With $B$ fixed, the $A$-subproblem is
\begin{equation}
\label{eq:A_subproblem}
\min_{A \in \stiefel(p,r)}
-\tr(A^\top \Sigmahat A)
+ \frac{\rho}{2}\|A-B\|_F^2.
\end{equation}
Its Euclidean gradient is
\begin{equation}
    \nabla_A f(A;B) = -2\Sigmahat A + \rho (A-B).
\end{equation}
The corresponding Riemannian gradient on $\stiefel(p,r)$ is
\begin{equation}
    \operatorname{grad} f(A;B)
    = \nabla_A f(A;B) - A\,\sym\!\big(A^\top \nabla_A f(A;B)\big).
\end{equation}
We take a fixed-step manifold descent update with step size $\eta_A$ and retract back to the Stiefel manifold using QR retraction.

\subsection{B-Step: Graph-Coupled Sparse Update}
With $A$ fixed, the $B$-subproblem is
\begin{equation}
\label{eq:B_subproblem}
\min_{B \in \R^{p \times r}}
\lambda_1 \|B\|_1
+ \lambda_2 \tr(B^\top L B)
+ \frac{\rho}{2}\|A-B\|_F^2.
\end{equation}
This subproblem is convex in $B$ because $L \succeq 0$. We use a proximal-gradient update that treats the Laplacian quadratic and coupling term as the smooth part and the $\ell_1$ penalty as the nonsmooth part. Define
\[
g(B) = \lambda_2 \tr(B^\top L B) + \frac{\rho}{2}\|A-B\|_F^2,
 \qquad
h(B) = \lambda_1 \|B\|_1.
\]
Then
\[
\nabla g(B) = 2\lambda_2 L B + \rho(B-A).
\]
With step size $\eta_B = 1 / (2\lambda_2 \|L\|_2 + \rho)$, the update is
\[
B^{(t+1)} = \soft\!\big(B^{(t)} - \eta_B \nabla g(B^{(t)}), \eta_B \lambda_1\big).
\]

\subsection{Alternating Scheme}
Algorithm~\ref{alg:alt} summarizes the outer loop.

\begin{algorithm}[t]
\caption{Alternating Manifold + Graph-Coupled Sparse Updates}
\label{alg:alt}
\begin{algorithmic}[1]
\Require Centered data $X$, graph Laplacian $L$, parameters $\lambda_1,\lambda_2,\rho$, rank $r$
\State Initialize $A^{(0)} \in \stiefel(p,r)$, $B^{(0)} \in \R^{p\times r}$
\For{$t=0,1,\dots,T-1$}
\State Update $A^{(t+1)}$ with one Riemannian gradient step and QR retraction
\State Update $B^{(t+1)}$ with one proximal-gradient step and soft-thresholding
    \If{stopping criterion satisfied}
        \State \textbf{break}
    \EndIf
\EndFor
\State \Return $(A^{(T)},B^{(T)})$
\end{algorithmic}
\end{algorithm}

\paragraph{Complexity.}
The cost of the $A$-step is dominated by matrix multiplications and the chosen retraction. The $B$-step is typically the computational bottleneck because graph regularization introduces feature coupling and removes the closed-form separability available in plain soft-thresholding. When $L$ is sparse, matrix-vector operations remain efficient; dense or ill-conditioned graph operators can substantially increase runtime.
Graph regularization increases computational cost due to feature coupling, particularly for dense or ill-conditioned graphs.

\section{Optimization Discussion}
\label{sec:theory}

The objective in \eqref{eq:main_obj} is nonconvex because it combines a Stiefel constraint on $A$ with a quadratic coupling term between $A$ and $B$. For fixed $A$, however, the $B$-subproblem is convex because it is the sum of a convex quadratic term and the entrywise $\ell_1$ norm.

\subsection{Updates Used}
The implemented solver alternates between two explicit updates. The $A$-step takes one retracted Riemannian gradient step on $\stiefel(p,r)$ using the Euclidean gradient from the manifold objective and QR retraction back to the manifold. The $B$-step takes one proximal-gradient step, with the smooth part given by the Laplacian quadratic and coupling term and the nonsmooth part given by $\lambda_1 \|B\|_1$.

The step size for the $A$-step is fixed at $\eta_A=0.05$. This value is conservative relative to the Lipschitz bound for the smooth $A$-subproblem (on the order of $2\|\Sigmahat\|_2 + \rho$), and was not tuned to optimize evaluation metrics. The $B$-step uses
\[
\eta_B = \frac{1}{2\lambda_2 \|L\|_2 + \rho},
\]
which matches the Lipschitz constant of the smooth part in the Euclidean update. We compute $\|L\|_2$ as the largest eigenvalue of $L$ (via \texttt{eigvalsh}).

\subsection{Stopping Criteria}
The outer loop stops when any of the following is satisfied (defaults used in all reported runs):
\begin{itemize}[leftmargin=*]
    \item relative objective change $\le 10^{-6}$,
    \item coupling gap $\|A-B\|_F / \max(1,\|A\|_F) \le 10^{-4}$,
    \item orthogonality defect $\|A^\top A - I_r\|_F \le 10^{-6}$,
    \item maximum iteration count $T=200$ is reached.
\end{itemize}

This section is intentionally descriptive rather than a proof development: we do not provide a formal convergence proof here, and formal analysis is out of scope for this manuscript.

\section{Experimental Setup}
\label{sec:experiments}

We evaluate only the executed synthetic study. The comparison set includes PCA, A-ManPG, GraphOnlyPCA ($\lambda_1=0$), a non-manifold graph+sparsity baseline (GraphSparseNoOrth), SparseNoGraph, and the Proposed method. SparseNoGraph is the in-family ablation with $\lambda_2=0$, while A-ManPG is the minimal external sparse orthogonal baseline without graph coupling. Clean-graph robustness results focus on chain and SBM, while misspecification experiments additionally include grid and kNN topologies. Main robustness results use seeds $0$--$4$, while sweep experiments and the graph-aligned setting use seeds $0$--$2$. Shared evaluation metrics only are reported: support recovery, graph smoothness on the true graph, shared explained variance, and support connectivity. For sparse baselines, we select $\lambda_1$ from a small grid to match the sparsity level (nnz) of the Proposed method for primary comparisons.

\paragraph{Metric mapping and definitions.}
Support metrics (precision/recall/F1 on the union and per component) are computed on $B$ after component matching to the true loadings; we use a Hungarian matching and sign correction before thresholding. Graph smoothness is reported as the normalized Laplacian energy
\[
\mathrm{Smoothness}(B;L) = \frac{\tr(B^\top L B)}{\tr(B^\top B)},
\]
computed on the true graph Laplacian. Shared explained variance is computed on an orthonormalized loading matrix: for Proposed, GraphOnlyPCA, GraphSparseNoOrth, and SparseNoGraph we use $Q=\mathrm{qr}(B)$, for A-ManPG we use $Q=\mathrm{qr}(A)$, and for PCA we use the top-$r$ eigenvectors of $\Sigmahat$. Support connectivity is computed on the union support mask (entries of $B$ exceeding $10^{-8}$) and reported as the number of connected components and the largest-component ratio.

\paragraph{A-ManPG settings.}
We use the public A-ManPG baseline with step size $\eta_A=0.05$, maximum iterations $200$, and tolerances $\text{tol}_{\mathrm{obj}}=10^{-6}$ and $\text{tol}_{\mathrm{orth}}=10^{-6}$, initialized from the PCA solution.

\subsection{Main Runs}
The main runs use the following hyperparameters unless otherwise stated:
\begin{itemize}[leftmargin=*]
    \item $\lambda_1=0.1$,
    \item $\lambda_2=0.1$,
    \item $\rho=5.0$,
    \item $\eta_A=0.05$.
\end{itemize}

\subsection{Graph-Aligned Chain Setting}
To test when the graph prior is genuinely helpful, we introduce a graph-aligned chain regime. Each component has a disjoint contiguous support block, evenly spaced along the chain with clear gaps, and we add scattered high-variance decoy features (20 non-support nodes with variance inflated by a factor of 2 relative to $\sigma^2$). This construction creates a setting where graph-unaware sparsity can be drawn toward isolated high-variance nodes, while graph regularization should favor coherent contiguous supports.

\subsection{Multi-Seed \texorpdfstring{$\lambda_2$}{lambda2} Sweeps}
We report multi-seed sweeps over $\lambda_2 \in \{0.0, 0.05, 0.1, 0.2, 0.5\}$ for SBM (mandatory) and for chain (recommended), using seeds $0$--$2$. We plot mean curves with uncertainty bands and overlay A-ManPG as a horizontal reference line (mean across the same seed set).

\section{Results}
\label{sec:results}

\subsection{Overview}
We report synthetic results using shared evaluation metrics across all methods: union support F1, graph smoothness norm on the true graph, shared explained variance (after QR orthonormalization), and a support-connectivity diagnostic (largest connected-component ratio). Shared explained variance is an evaluation metric and is not the training objective for any method. We do not compare raw objective values across methods because they optimize different objectives. Results are presented for chain and SBM graphs across five seeds (0--4), and for multi-seed $\lambda_2$ sweeps (seeds 0--2). The comparison set includes PCA, A-ManPG, GraphOnlyPCA ($\lambda_1=0$), a non-manifold graph+sparsity baseline (GraphSparseNoOrth), SparseNoGraph, and the Proposed method. Primary comparison uses matched sparsity; secondary appendix comparison uses each baseline tuned within its own family under a fixed validation protocol.

\subsection{Chain Robustness (Seeds 0--4)}
Across seeds, PCA is consistently worst on support recovery. A-ManPG improves substantially over PCA but remains below the sparse baselines on mean support F1. GraphOnlyPCA yields smoother loadings but remains dense and therefore weak on support recovery, while the non-manifold GraphSparseNoOrth baseline underperforms on recovery and variance. SparseNoGraph achieves the highest mean support F1, while the Proposed method attains slightly lower F1 but achieves a lower graph smoothness norm on the true chain graph. Shared explained variance is similar across the non-PCA methods and lower than PCA, as expected for sparse baselines. This pattern indicates that on chain graphs, graph regularization improves structural coherence while remaining competitive on recovery.
\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\caption{Chain robustness (seeds 0--4). Mean $\pm$ std for shared evaluation metrics.}
\label{tab:chain-summary}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lccc}
\toprule
Method & Support F1 & Smoothness (norm) & Shared explained var \\
\midrule
PCA & 0.261 $\pm$ 0.000 & 2.099 $\pm$ 0.484 & 6.225 $\pm$ 0.406 \\
A-ManPG & 0.862 $\pm$ 0.093 & 2.111 $\pm$ 0.484 & 6.130 $\pm$ 0.406 \\
GraphOnlyPCA & 0.261 $\pm$ 0.000 & 1.893 $\pm$ 0.429 & 6.191 $\pm$ 0.402 \\
GraphSparseNoOrth & 0.261 $\pm$ 0.000 & 1.993 $\pm$ 0.559 & 4.364 $\pm$ 0.966 \\
SparseNoGraph & 0.917 $\pm$ 0.072 & 2.120 $\pm$ 0.483 & 6.050 $\pm$ 0.364 \\
Proposed & 0.927 $\pm$ 0.019 & 1.871 $\pm$ 0.465 & 6.059 $\pm$ 0.415 \\
\bottomrule
\end{tabular}%
}
\end{table}

\subsection{SBM Robustness (Seeds 0--4)}
On SBM graphs, PCA again performs worst on support recovery. A-ManPG again improves over PCA but does not match the sparse baselines on support recovery. GraphOnlyPCA attains lower smoothness values but remains dense, and GraphSparseNoOrth exhibits substantially weaker recovery and variance. SparseNoGraph attains substantially higher support F1 than the Proposed method, while the Proposed method is consistently smoother on the true SBM graph (lower normalized graph smoothness). Shared explained variance remains comparable across the sparse methods. These results show a stable recovery--smoothness tradeoff on SBM: stronger graph regularization improves structural smoothness but can reduce recovery accuracy.
\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\caption{SBM robustness (seeds 0--4). Mean $\pm$ std for shared evaluation metrics.}
\label{tab:sbm-summary}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lccc}
\toprule
Method & Support F1 & Smoothness (norm) & Shared explained var \\
\midrule
PCA & 0.261 $\pm$ 0.000 & 17.085 $\pm$ 0.974 & 6.225 $\pm$ 0.406 \\
A-ManPG & 0.862 $\pm$ 0.093 & 17.135 $\pm$ 1.018 & 6.130 $\pm$ 0.406 \\
GraphOnlyPCA & 0.261 $\pm$ 0.000 & 13.176 $\pm$ 2.192 & 5.755 $\pm$ 0.448 \\
GraphSparseNoOrth & 0.209 $\pm$ 0.117 & 11.504 $\pm$ 6.686 & 2.981 $\pm$ 1.784 \\
SparseNoGraph & 0.965 $\pm$ 0.028 & 17.156 $\pm$ 1.042 & 6.103 $\pm$ 0.409 \\
Proposed & 0.744 $\pm$ 0.051 & 15.764 $\pm$ 1.104 & 6.003 $\pm$ 0.384 \\
\bottomrule
\end{tabular}%
}
\end{table}

\subsection{Graph-Aligned Chain Setting (Disjoint Supports + Scattered Decoys)}
Figure~\ref{fig:graph-aligned-sweep} summarizes the graph-aligned chain experiment. Moderate $\lambda_2$ keeps support recovery within a small F1 margin of the strongest sparse baselines while improving both smoothness and support connectivity. Table~\ref{tab:graph-aligned-summary} reports a representative $\lambda_2=0.1$ setting, showing that the Proposed method remains within roughly 1--2 F1 points while improving structural connectivity and smoothness.
\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{graph_aligned_lambda2_sweep_panel.png}
\caption{Graph-aligned chain sweep (seeds 0--2). Proposed and SparseNoGraph are shown with uncertainty bands; A-ManPG is a horizontal reference line.}
\label{fig:graph-aligned-sweep}
\end{figure}

\begin{table}[t]
\centering
\scriptsize
\setlength{\tabcolsep}{3pt}
\caption{Graph-aligned chain (representative $\lambda_2=0.1$, seeds 0--2). Mean $\pm$ std for shared metrics.}
\label{tab:graph-aligned-summary}
\resizebox{\linewidth}{!}{%
\begin{tabular}{lccc}
\toprule
Method & Support F1 & Smoothness (norm) & Largest CC ratio \\
\midrule
PCA & 0.261 $\pm$ 0.000 & 2.047 $\pm$ 0.472 & 1.000 $\pm$ 0.000 \\
A-ManPG & 0.790 $\pm$ 0.041 & 2.048 $\pm$ 0.474 & 0.242 $\pm$ 0.032 \\
GraphOnlyPCA & 0.261 $\pm$ 0.000 & 1.863 $\pm$ 0.484 & 1.000 $\pm$ 0.000 \\
GraphSparseNoOrth & 0.261 $\pm$ 0.000 & 2.144 $\pm$ 0.616 & 1.000 $\pm$ 0.000 \\
SparseNoGraph & 0.896 $\pm$ 0.044 & 2.074 $\pm$ 0.405 & 0.321 $\pm$ 0.063 \\
Proposed & 0.914 $\pm$ 0.016 & 1.810 $\pm$ 0.456 & 0.377 $\pm$ 0.014 \\
\bottomrule
\end{tabular}%
}
\end{table}

\subsection{Multi-Seed \texorpdfstring{$\lambda_2$}{lambda2} Sweeps}
Sweeping $\lambda_2 \in \{0.0, 0.05, 0.1, 0.2, 0.5\}$ across seeds (0--2) reveals stable smoothness--recovery tradeoffs with topology-dependent sensitivity. On SBM, stronger regularization steadily reduces support F1 while improving smoothness; light regularization improves smoothness with limited recovery loss. On chain graphs, the same sweep shows a milder recovery penalty for comparable smoothness gains. In all sweeps, A-ManPG provides a fixed sparse baseline reference line.
These clean-graph results serve as a baseline; we next examine how graph misspecification alters this behavior.
\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{sbm_lambda2_sweep_panel.png}
\caption{SBM $\lambda_2$ sweep (seeds 0--2). Curves show mean $\pm$ std across seeds; A-ManPG is a horizontal reference line.}
\label{fig:sbm-lambda2-sweep}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{chain_lambda2_sweep_panel.png}
\caption{Chain $\lambda_2$ sweep (seeds 0--2). Curves show mean $\pm$ std across seeds; A-ManPG is a horizontal reference line.}
\label{fig:chain-lambda2-sweep}
\end{figure}

\subsection{Topology-Dependent Robustness Under Misspecification}
We now study how these behaviors change when the graph prior is misspecified.
To study the effect of graph misspecification, we apply controlled edge rewiring at corruption level $\alpha$ and evaluate performance relative to the clean graph baseline at the same $\lambda_2$. Figure~\ref{fig:corruption_phase} presents phase diagrams over $(\lambda_2,\alpha)$ for different graph topologies.

We observe a clear topology-dependent pattern. For the chain graph, performance degrades substantially as both $\lambda_2$ and $\alpha$ increase, indicating that sparse, fragile structures are highly sensitive to misspecification. In contrast, grid, SBM, and kNN graphs exhibit markedly greater robustness: performance remains stable across a wide range of corruption levels, and in some regimes shows slight improvements relative to the clean graph.
\textbf{This demonstrates that robustness to graph misspecification is not uniform, but fundamentally governed by graph redundancy.}
We use ``fragile'' to denote low-redundancy graphs (e.g., chains) where connectivity relies on few edges, and ``redundant'' to denote graphs with multiple alternative paths (e.g., grids, SBM, kNN).

This contrast is summarized in Figure~\ref{fig:corruption_summary}, which reports the change in support F1 at a representative high-regularization setting ($\lambda_2=0.5,\alpha=0.4$). The chain graph shows a clear degradation ($-0.097 \pm 0.021$), whereas grid ($+0.041 \pm 0.019$), SBM ($+0.021 \pm 0.013$), and kNN ($+0.047 \pm 0.014$) exhibit little degradation or slight improvement.

These results suggest that the effect of graph regularization under misspecification depends critically on graph topology. Redundant graphs (e.g., grid, SBM, kNN) provide multiple alternative paths that mitigate the impact of edge perturbations, and can even alleviate oversmoothing by weakening overly strong regularization. In contrast, fragile graphs such as chains lack redundancy, so perturbations directly disrupt the structure enforced by the regularizer, leading to degraded recovery.

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{corruption_phase_panels.png}
\caption{Topology-dependent sensitivity of graph-regularized sparse PCA under misspecification. Each panel shows the change in support F1 ($\Delta$F1) relative to the clean graph (same $\lambda_2$) as a function of $\lambda_2$ and corruption level $\alpha$, averaged over seeds. Fragile topologies (chain) exhibit pronounced degradation at high $\lambda_2$ and $\alpha$, while redundant graphs (grid, SBM, kNN) are substantially less sensitive and can show slight improvements consistent with reduced oversmoothing. The shared color scale is centered at zero.}
\label{fig:corruption_phase}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{corruption_delta_summary.png}
\caption{Change relative to the clean graph at a representative high-regularization setting ($\lambda_2 = 0.5, \alpha = 0.4$). Chain shows a clear degradation, whereas grid, SBM, and kNN exhibit little degradation or slight improvement consistent with reduced oversmoothing. Bars show mean $\Delta$F1 over seeds; error bars indicate standard error.}
\label{fig:corruption_summary}
\end{figure}

\clearpage
\subsection{Real-Data Trends}
We report $\lambda_2$ sweeps on TCGA-BRCA (STRING), MNIST (grid), and S\&P500 (correlation) to assess how the graph prior affects variance--smoothness tradeoffs in realistic settings (Figure~\ref{fig:realdata-unified}).
These trends are consistent with the topology-dependent behavior observed in synthetic experiments: MNIST (grid-like) exhibits stable performance across $\lambda_2$, TCGA-BRCA (modular) shows moderate sensitivity, and S\&P500 (noisy correlation graph) is more sensitive at higher $\lambda_2$.
These results suggest that real-world graphs behave similarly to their synthetic analogues: grid-like structures (MNIST) are robust, while noisier correlation graphs (S\&P500) are more sensitive to regularization strength.
To deepen the TCGA-BRCA case, we assess stability across seeds and graph-coherence of selected supports against both uniform and degree-matched nulls (Figure~\ref{fig:tcga-stability-coherence}).

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{../figures/realdata/realdata_unified_panels.png}
\caption{Unified real-data $\lambda_2$ sweeps on TCGA-BRCA (STRING), MNIST (grid), and S\&P500 (correlation). Curves show mean trends across seeds for explained variance, graph smoothness, and sparsity.}
\label{fig:realdata-unified}
\end{figure}

\begin{figure}[t]
\centering
\includegraphics[width=\linewidth]{../figures/realdata/tcga_stability_coherence.png}
\caption{TCGA-BRCA deep dive at $\lambda_2=0.1$ (Proposed). Left: seed-to-seed stability of the learned supports and subspaces. Right: graph-coherence diagnostics for the selected supports compared to uniform and degree-matched random gene sets, showing elevated density and average degree relative to nulls.}
\label{fig:tcga-stability-coherence}
\end{figure}

\clearpage
\section{Discussion and Limitations}
\label{sec:discussion}

These experiments establish a focused empirical message: in our synthetic experiments, graph regularization yields smoother solutions on the true graph for both chain and SBM settings, but its effect on support recovery is topology-dependent. On chain graphs, the proposed method remains competitive on recovery while producing smoother supports, consistent with the intuition that chain structure aligns well with Laplacian smoothness. On SBM graphs, the graph prior enforces smoother structure at the cost of recovery accuracy, yielding a stable tradeoff rather than a contradiction. In the graph-aligned chain setting with disjoint contiguous supports and scattered high-variance decoys, moderate regularization preserves recovery while improving support connectivity, indicating a setting where the graph prior is beneficial for structural fidelity.

The chain setting aligns naturally with local smoothness, whereas the SBM setting emphasizes block/community structure; as a result, the same smoothness penalty can act as a mild regularizer in one topology and as an oversmoothing force in another. The $\lambda_2$ sweep on SBM makes this explicit: mild regularization yields smoother solutions with limited recovery loss, whereas stronger regularization oversmooths and reduces recovery. These observations imply that $\lambda_2$ must be tuned with respect to graph topology rather than assumed to be universally beneficial.

The main limitation of the current evidence is that it remains synthetic and focuses on two graph families with a single data generator. This is appropriate for a focused empirical study, whose goal is to establish a disciplined method and a clear empirical tradeoff. The results demonstrate that structural priors can improve graph coherence, but they also show that regularization strength must be tuned to topology. If the graph-aligned setting fails to preserve recovery, the correct conclusion is a tradeoff characterization rather than a recovery-improving claim.

\paragraph{Threats to validity.}
Despite expanded graph families, our experiments still cover a limited subset of possible topologies. The S\&P500 correlation graph is a noisy proxy for true relationships, and real data lacks ground-truth supports. Implementations share a common NumPy codebase and are not equivalently optimized, so runtime comparisons are indicative only. Some solver calls produced runtime warnings in stressed regimes; all outputs were logged and checked for finite reported metrics, and warning-aware logging was used to prevent silent corruption. Finally, hyperparameter tuning choices can influence regime boundaries.

\section{Conclusion}
\label{sec:conclusion}

We present controlled empirical evidence that graph-regularized sparse orthogonal PCA improves structural coherence while inducing a topology-dependent tradeoff with support recovery. On chain graphs, the proposed method is competitive on recovery and yields smoother supports; on SBM graphs, stronger graph regularization improves smoothness but reduces recovery accuracy. In a graph-aligned chain setting with disjoint contiguous supports and scattered decoys, moderate regularization preserves recovery while improving support connectivity. These findings highlight the need for topology-aware $\lambda_2$ tuning and support a modest, defensible claim: graph regularization improves structural coherence, and its recovery impact depends on topology and regularization strength.

A key takeaway is that the effect of graph regularization depends critically on graph topology. Sparse, fragile graphs such as chains are highly sensitive to misspecification, while redundant graphs such as grids, SBM, and kNN are more robust and can even alleviate oversmoothing in certain regimes. This suggests that the usefulness of graph priors in sparse PCA is not universal, but depends on structural redundancy in the underlying graph. Understanding this distinction is essential when applying graph-regularized methods in practice. While our study focuses on PCA, the observed topology-dependent behavior is likely relevant to other graph-regularized learning problems where structural priors interact with sparsity.

\fussy
\bibliographystyle{plain}
\bibliography{references}

\end{document}
