[project]
name = "topospca"
version = "0.1.0"
description = "Topology-Dependent Robustness in Graph-Regularized Sparse PCA"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
  "numpy",
  "pandas",
  "scipy",
  "pyyaml",
  "matplotlib",
  "pytest",
]
